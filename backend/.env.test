# Variables de entorno para tests
NODE_ENV=test
JWT_SECRET=test-secret-key-for-testing
JWT_EXPIRES_IN=1d
FRONTEND_URL=http://localhost:3000

# Base de datos de test (usar BD de test o mock)
DATABASE_URL=
DB_HOST=localhost
DB_PORT=5432
DB_NAME=repartos_si_test
DB_USER=test
DB_PASSWORD=test

# Pusher (mock en tests)
PUSHER_APP_ID=test
PUSHER_KEY=test
PUSHER_SECRET=test
PUSHER_CLUSTER=test
