# Configuración de Base de Datos PostgreSQL
# Para Vercel, usar una BD en la nube: Neon, Supabase, Railway, etc.
DB_HOST=your-db-host.neon.tech
DB_PORT=5432
DB_NAME=repartos_si
DB_USER=your_username
DB_PASSWORD=your_password

# Configuración del servidor
PORT=5000
NODE_ENV=production

# JWT Secret - Usar un string largo y aleatorio
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# URL del Frontend (actualizar con tu URL de Vercel)
FRONTEND_URL=https://tu-frontend.vercel.app

# Configuración de Pusher (para WebSockets en serverless)
# Crear cuenta gratis en https://pusher.com
PUSHER_APP_ID=your_app_id
PUSHER_KEY=your_key
PUSHER_SECRET=your_secret
PUSHER_CLUSTER=us2

# Configuración de Email (opcional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=tu-email@gmail.com
EMAIL_PASS=tu-app-password

# Database URL completa (alternativa, algunas plataformas la usan)
# DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require
